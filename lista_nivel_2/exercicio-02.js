// Número negativo ou positivo

const negativoOuPositivo = numero => {
  return numero >= 0 ? console.log(`O ${numero} é positivo`) : console.log(`O ${numero} é negativo`);
}

// Chama a função para retornar se negativo ou positivo

negativoOuPositivo(0);